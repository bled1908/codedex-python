# Built - in functions previously used
rating = float(input("Enter Rating: ")) #converts the string input to float
name = input("Enter your name: ") #helps in taking string input
favourite_number = int(input("Enter your favourite number: ")) #converts the string input to int
name_len = len(name) #calculate the length of the object

# built-in function that we have not seen before.
a = ("a", "b", "c", "d", "e", "f", "g", "h")

x = slice(4)

print(a[x])
# output - ('a', 'b', 'c', 'd')
# give the first n elements of the sequence